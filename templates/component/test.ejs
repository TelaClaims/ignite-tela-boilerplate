import React from 'react'
// import { shallow } from 'enzyme'
import renderer from 'react-test-renderer'
import Component from './'

// default props
let props, component

beforeEach(() => {
    component = null
    props = { }
})

describe('<%= props.name %>Component', () => {
    describe('Snapshot', () => {
        test('Renders', () => {
            component = renderer.create(<Component {...props}/>)
            const tree = component.toJSON()
            expect(tree).toMatchSnapshot();
        })
    })

    <%if (props.type !== 'Stateless') { %>
    // describe('Lifecycle' () => { })
    <% } %>

    // describe('Methods', () => {
    //     beforeEach(() => {
    //         component = shallow(<Component {...props}/>).instance()
    //     })
    // })
})
